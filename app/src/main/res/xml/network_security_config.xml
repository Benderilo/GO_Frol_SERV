<?xml version="1.0" encoding="utf-8"?>
<!--
  Приложение ходит только по HTTPS: боевой сервер работает с сертификатом,
  а незашифрованный запрос система теперь просто не выпустит.
  Исключение оставлено для локальной разработки: 10.0.2.2 — это хост-машина
  с точки зрения эмулятора.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
